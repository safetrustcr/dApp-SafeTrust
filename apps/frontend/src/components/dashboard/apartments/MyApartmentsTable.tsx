// TODO: wire to Hasura query — SELECT * FROM public.apartments WHERE owner_address = $address

export function MyApartmentsTable() {
  return (
    <div className="rounded-md border p-4 text-sm text-muted-foreground">
      My Apartments — coming soon
    </div>
  );
}
